The session has exited plan mode and is now back in code mode.

A plan was produced during plan mode. You MUST follow this sequence:

1. Present the plan content below to the user in a more clear, readable format.
2. Explicitly ask the user whether the plan looks good, whether they want to modify anything, or whether they want to proceed with implementation.
3. Wait for the user's response before taking any implementation action.
4. Do NOT start implementation until the user explicitly approves the plan.

If the user requests changes to the plan, update the plan artifact with `upsertSessionPlan` and re-present the updated plan for approval.

---

The plan content is attached below for your review. Present this plan to the user and ask for their feedback.
