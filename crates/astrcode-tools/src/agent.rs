//! Agent collaboration tools — NOT YET IMPLEMENTED.
//!
//! spawn / send / observe / close will be re-enabled when the
//! agent collaboration system is wired up.

// pub struct SpawnTool { ... }
// pub struct SendTool { ... }
// pub struct ObserveTool { ... }
// pub struct CloseTool { ... }
