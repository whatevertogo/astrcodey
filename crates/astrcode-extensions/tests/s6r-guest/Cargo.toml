[package]
name = "s6r-guest-demo"
version = "0.1.0"
edition = "2021"

# 不加入 workspace，避免影响主项目编译
[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]
